/**
 * Storage key used to persist the current user session.
 * Typically used for accessing session-related data
 * in localStorage, sessionStorage, or similar mechanisms.
 *
 * @constant {string}
 */
export const SESSION = "session";
